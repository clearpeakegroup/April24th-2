# DO NOT REMOVE: All dependencies must be pinned and kept in sync with Docker builds and root requirements.txt.
# If you add or update a package, check for transitive dependencies and version conflicts.
# All builds must pass `pip check` and update requirements.lock after a successful build.
# See requirements.lock for the exact versions used in production.
rq==1.16.1
aioredis==2.0.1
pytest==8.3.5
pytest-asyncio
hypothesis==6.100.0
pandas==2.2.3
sqlalchemy==2.0.30 